/**
 * Authentication Module Configuration
 *
 * Configures all authentication-related services and their dependencies
 * using the enhanced dependency injection system.
 *
 * TODO: This module has structural issues and needs to be refactored
 */

import { Module } from '../decorators.js';

/**
 * Authentication module stub - TODO: Implement proper auth services
 */
@Module({
  name: 'AuthModule',
  imports: [],
  providers: [
    // TODO: Add authentication providers when auth module is fixed
  ],
  exports: [],
  global: true,
})
export class AuthModule {}
