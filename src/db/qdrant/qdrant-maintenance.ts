import { type QdrantClient } from '@qdrant/js-client-rest';
export async function optimize(client: QdrantClient) {
  /* implementation */
}
export async function validate(client: QdrantClient) {
  /* implementation */
}
export async function backup(client: QdrantClient, destination: string) {
  /* implementation */
}
