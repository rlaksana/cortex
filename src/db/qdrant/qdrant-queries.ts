import { type QdrantClient } from '@qdrant/js-client-rest';
export async function search(client: QdrantClient, query: string, options: unknown) {
  return [];
}
export async function semanticSearch(client: QdrantClient, query: string, options: unknown) {
  return [];
}
export async function exactSearch(client: QdrantClient, query: string, options: unknown) {
  return [];
}
