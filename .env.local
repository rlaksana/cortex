# Local Development Environment - 2-Layer Architecture
# Direct connection: Local Node.js → Docker PostgreSQL (port 5433)

# Database Connection (Direct Docker PostgreSQL)
DATABASE_URL=postgresql://cortex:cortex_pg18_secure_2025_key@localhost:5433/cortex_prod
DB_HOST=localhost
DB_PORT=5433
DB_NAME=cortex_prod
DB_USER=cortex
DB_PASSWORD=cortex_pg18_secure_2025_key

# Environment
NODE_ENV=development
LOG_LEVEL=info

# Performance Tuning (Optimized for local development)
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_IDLE_TIMEOUT_MS=30000
DB_CONNECTION_TIMEOUT_MS=10000
DB_QUERY_TIMEOUT=30000
DB_STATEMENT_TIMEOUT=30000

# MCP Server Configuration (Local execution)
MCP_SERVER_NAME=cortex-memory
MCP_SERVER_VERSION=1.1.0
MCP_MAX_BATCH_SIZE=100
MCP_TRANSPORT=stdio

# Scope Configuration (Optional - defaults to git context)
CORTEX_ORG=
CORTEX_PROJECT=cortex
CORTEX_BRANCH=001-create-specs-000