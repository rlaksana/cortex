# Test Environment Configuration for MCP Cortex Memory
# Copy this file to .env.test and configure as needed

# Database Configuration
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION_NAME=cortex-test

# OpenAI Configuration (for embeddings tests)
OPENAI_API_KEY=your-test-api-key-here

# Semantic Chunking Configuration
# When set to true, disables semantic chunking and uses traditional structural chunking only
# This is useful for environments where embedding services are unavailable or unreliable
SEMANTIC_CHUNKING_OPTIONAL=false

# Test Configuration
NODE_ENV=test
LOG_LEVEL=error
ENABLE_CACHING=false
ENABLE_METRICS=false
MOCK_EXTERNAL_SERVICES=true

# Security Configuration (not required for tests)
# JWT_SECRET=test-jwt-secret-at-least-32-chars
# ENCRYPTION_KEY=test-encryption-key-at-least-32-chars